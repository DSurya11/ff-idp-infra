# Copy this file to terraform.tfvars and fill in real values.
# terraform.tfvars is gitignored — never commit real API keys.

neon_api_key    = "your-neon-api-key-here"
neon_project_id = "your-neon-project-id-here"
